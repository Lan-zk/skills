# 综合测试文档

这是一个包含所有支持元素的综合测试文档。

## 标题层级测试

### 三级标题

这是三级标题下的内容。

#### 四级标题（不支持，显示为普通文本）

四级标题内容。

## 段落与行内样式

这是一段包含各种**粗体**、*斜体*和`行内代码`的文字。我们还测试混合使用：这是**粗体与*斜体*组合**，以及`代码中的**粗体**`。

## 无序列表

- 列表项一
- 列表项二
  - 嵌套项 A
  - 嵌套项 B
- 列表项三

## 有序列表

1. 第一步
2. 第二步
3. 第三步
   1. 子步骤 3.1
   2. 子步骤 3.2

## 引用块

> 这是一段引用文字。
>
> 可以包含多个段落。
>
> - 也可以包含列表

## 分隔线

上面是分隔线，下面是更多内容。

## 代码块

### JavaScript

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet('World');
```

### Python

```python
def greet(name):
    print(f"Hello, {name}!")

greet("World")
```

### 无语言代码块

```
plain text code
no syntax highlighting
```

## 长段落测试

这是一段非常长的段落内容，用于测试自动分页功能。我们需要确保长段落能够被正确地拆分成多页，同时保持段落内容的完整性和可读性。在拆分过程中，必须确保行内样式（如**粗体**、*斜体*、`行内代码`）能够正确保留。此外，还需要测试中文、英文、数字混合内容的分页效果。

继续添加更多内容来确保分页触发。这里是第三段文字，包含一些技术术语和特殊符号：数组 [1, 2, 3]、对象 {key: value}、URL https://example.com/path?query=参数，以及各种标点符号！@#$%^&*()_+-=[]{}|;':",./<>?。

## 图片测试

![示例图片](./images/example.png)

## 表格测试（不支持）

| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据 | 数据 | 数据 |

表格不支持，将显示为提示文本。

## 数学公式测试（不支持）

$$E = mc^2$$

数学公式不支持，将显示为提示文本。

## 任务列表测试（不支持）

- [ ] 未完成任务
- [x] 已完成任务

任务列表不支持，将显示为普通列表。

---

这是文档结尾。
